``markdown # Ivan Reseller Web — Phase 2 Implementation and Vercel Deployment Repair You are continuing development of the production system **Ivan Reseller Web**. The system currently runs in production with the following infrastructure: Backend (Node.js / TypeScript) on Railway PostgreSQL on Railway Redis on Railway BullMQ workers inside the API process The frontend is a **React + Vite SPA** and is deployed on **Vercel**. Phase 1 systems are already implemented and documented in: docs/DEPLOYMENT_REPAIR_AND_PHASE2.md You must now: 1. Continue implementing Phase 2 features 2. Diagnose and repair the frontend deployment on Vercel 3. Ensure stable production deployment You must perform these tasks autonomously. Do not give manual instructions unless absolutely unavoidable. --- # CRITICAL RULES Do NOT remove existing functionality. Do NOT refactor core architecture unnecessarily. Only extend and stabilize the system. Existing Phase 1 systems must remain intact. --- # CURRENT SYSTEM CAPABILITIES The system already includes: Inventory Sync Service Listing Metrics system Analytics API Listing Optimization Worker Marketplace Rate Limiting BullMQ workers Redis queue system Prisma/PostgreSQL database Metrics are written to `listing_metrics`. Workers run inside the API process. Backend deployment is on Railway. Frontend deployment is currently on Vercel. --- # PHASE 1 VERIFICATION Verify that the following systems exist and remain operational: Inventory Sync Service Listing Metrics table Analytics API `/api/analytics/listings` Listing Optimization Worker Marketplace Rate Limiting Confirm Redis/BullMQ initialization and Prisma migrations. Do not modify Phase 1 behavior. --- # VERCEL DEPLOYMENT DIAGNOSIS Analyze the current Vercel build output. Observed warning: Node version mismatch: package.json defines "engines": { "node": "24.x" } while Vercel project settings define Node 22. Determine whether Node 24 is actually required. If not required: update the frontend `package.json` to use a stable LTS version: Node 20 or Node 22. Ensure the Vercel environment uses the same version. --- # VERIFY FRONTEND BUILD CONFIGURATION The frontend lives in: frontend/ Confirm the correct configuration for Vercel: Root directory: frontend Build command: npm run build Output directory: dist Ensure Vite builds correctly. Verify that `npm run build` produces a valid SPA in `dist/`. --- # VERIFY FRONTEND START CONFIGURATION Vercel should serve the built SPA statically. Ensure the project uses the correct configuration: If necessary create a `vercel.json` file. Example structure: routes to serve index.html for SPA routing. Ensure the deployment serves the built Vite application correctly. --- # VERIFY ENVIRONMENT VARIABLES Check the frontend environment variables. Ensure the frontend API base URL points to the Railway backend. Example: VITE_API_URL Verify the frontend code reads this variable. Ensure the correct value is set in Vercel project settings. --- # PHASE 2 IMPLEMENTATION After verifying deployment stability, implement Phase 2 systems. --- # TASK 1 — PRODUCT RESEARCH UI Create a Product Research interface. Reuse backend services: opportunity-finder.service.ts competitor-analyzer.service.ts google-trends.service.ts Expose APIs if necessary. Frontend must allow: search products view demand signals view competition signals view estimated margins add product to opportunities Integrate with existing Opportunities workflow. --- # TASK 2 — IMAGE PROCESSING PIPELINE Implement an image processing pipeline before publishing listings. Pipeline tasks: download supplier images resize images (minimum 1200x1200) normalize format remove obvious watermarks when possible generate structured image sets Use a Node image library such as: sharp Integrate the pipeline with: marketplace.service.ts Ensure the pipeline runs before listing publication. --- # TASK 3 — MULTI SUPPLIER ARCHITECTURE Extend inventory sync architecture. Create a supplier abstraction layer. Example interface: SupplierAdapter Methods: getProductInfo getStock getPrice Convert existing AliExpress integration to: AliExpressSupplierAdapter Allow additional suppliers to be added later. Do not break existing AliExpress functionality. --- # TASK 4 — CTR / CLICK METRICS Extend the listing metrics system. Currently supported: impressions sales conversionRate Add support for: clicks When marketplace APIs provide click data (e.g. eBay analytics), store: listing_metrics.clicks Reuse existing metrics writer service. --- # TASK 5 — ANALYTICS UI EXTENSION Extend the analytics dashboard in the frontend. Add visualizations for: impressions clicks sales conversion rate Use the existing endpoint: GET /api/analytics/listings Add filtering by: marketplace date range --- # FINAL OBJECTIVE Ivan Reseller Web must continue evolving into a robust dropshipping automation SaaS platform. The system must maintain: stable Railway backend stable Vercel frontend deployment working BullMQ automation data-driven optimization All improvements must be additive and safe. ``` --- # Sobre el error que viste en Vercel Lo importante aquí es esta línea: ``` "engines": { "node": "24.x" } ``` Node 24 **no es necesario para Vite/React** y puede causar problemas. Lo normal es usar: ``` Node 20 ``` o ``` Node 22 ``` Cursor debería corregirlo automáticamente. --- # Qué pasará cuando ejecutes este prompt Cursor debería: 1️⃣ revisar **configuración Vercel** 2️⃣ arreglar **Node version mismatch** 3️⃣ verificar **build Vite** 4️⃣ crear **vercel.json si hace falta** 5️⃣ revisar **VITE_API_URL** 6️⃣ continuar **Phase 2** --- Si quieres, en el siguiente paso puedo darte **el prompt que convierte Ivan Reseller en un SaaS de dropshipping realmente dominante**, creando el **motor automático de detección de productos ganadores** que usan las plataformas más avanzadas.
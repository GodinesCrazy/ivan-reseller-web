/**
 * @deprecated P55 — Legacy workflow stage counts derived from coarse product flags.
 * Do not mount for operator truth; use Control Center and `/api/dashboard/operations-truth` surfaces instead.
 * Exported as no-op to avoid reintroducing duplicate lifecycle narratives.
 */
export default function WorkflowSummaryWidget() {
  return null;
}

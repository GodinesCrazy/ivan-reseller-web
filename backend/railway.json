{
  "build": {
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node scripts/railway-start-production.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 720
  }
}
